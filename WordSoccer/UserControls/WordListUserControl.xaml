<UserControl
    x:Class="WordSoccer.UserControls.WordListUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:WordSoccer.Game.Converters"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

	<StackPanel
		Background="#99ffffff"
		Margin="5">

		<ScrollViewer>

			<ListView
				x:Name="wordListView">

				<ListView.ItemTemplate>
					<DataTemplate>
						<Grid
						Background="#99ffffff">

							<Grid.Resources>
								<converters:StateConverter x:Key="StatePropertyConverter" />
							</Grid.Resources>

							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="Auto"/>
								<ColumnDefinition Width="*"/>
							</Grid.ColumnDefinitions>

							<TextBlock
							Grid.Column="0"
							Text="N"
							Padding="5"/>

							<TextBlock
							Grid.Column="1"
							Style="{Binding WordStateProperty, Converter={StaticResource StatePropertyConverter}}"
							Text="{Binding WordProperty}"
							Padding="5"/>

						</Grid>

					</DataTemplate>

				</ListView.ItemTemplate>

			</ListView>

		</ScrollViewer>
		
	</StackPanel>

</UserControl>